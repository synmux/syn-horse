# syn.horse — terminal UI kit

a fake CLI that mimics what running `synh` from your shell might look like. one html, one component file. interactive: type commands.

available commands: `help`, `ls`, `synth new`, `synth list`, `cat <id>`, `vibe`, `clear`, `exit`.
