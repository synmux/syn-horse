# to-do list

> _in order of implementation_

1. [ ] validate AI moderation
2. [ ] implement ntfy in `src/adapters/ntfy.ts` with `ntfy` package
3. [ ] use Cloudflare Workflow to orchestrate processing steps instead of homebrew
4. [ ] implement Pushover in `src/adapters/pushover.ts` with `pushover-js` package
5. [ ] implement email in `src/adapters/email.ts` with Cloudflare Email `EMAIL` binding
