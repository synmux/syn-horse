# to-do list

> _in order of implementation_

1. [ ] use Cloudflare Workflow to orchestrate processing steps instead of homebrew
2. [ ] implement Pushover in `src/adapters/pushover.ts` with `pushover-js` package
3. [ ] implement email in `src/adapters/email.ts` with Cloudflare Email `EMAIL` binding
